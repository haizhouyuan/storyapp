import { test } from '@playwright/test';

// TODO: 待真实语音服务接入后启用
// 当前阶段仅提供骨架，确保 pipeline 中可见 TTS 测试计划。
test.describe.skip('TTS 端到端冒烟', () => {
  test('用户可在故事页触发朗读按钮并看到状态变化', async ({ page }) => {
    // 预期步骤：
    // 1. 打开首页并输入主题
    // 2. 跳转至故事页后点击“播放朗读”按钮
    // 3. 校验按钮状态（loading → playing）以及字幕显示
  });

  test('故事树模式自动播放遵循用户偏好设置', async ({ page }) => {
    // 预期步骤：
    // 1. 开启语音自动播放偏好
    // 2. 在故事树页面生成节点，验证自动触发朗读
    // 3. 切换节点时自动暂停上一段
  });
});
