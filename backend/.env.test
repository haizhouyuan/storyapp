# 测试环境配置文件
NODE_ENV=test
PORT=5002

# 测试环境MongoDB（使用测试数据库）
# 注意：生产环境应从环境变量或密钥管理系统获取认证信息
MONGODB_URI=${MONGODB_TEST_URI:-mongodb://localhost:27017/storyapp_test}
MONGODB_DB_NAME=storyapp_test

# DeepSeek API配置（测试环境可以为空或使用模拟）
# DEEPSEEK_API_KEY=sk-test-dummy
DEEPSEEK_API_URL=https://api.deepseek.com

# 日志配置
ENABLE_DETAILED_LOGGING=false
ENABLE_DB_LOGGING=false
LOG_LEVEL=error

# 前端URL
FRONTEND_URL=http://localhost:3000